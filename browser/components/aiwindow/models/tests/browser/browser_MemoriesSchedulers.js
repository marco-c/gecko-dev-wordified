/
*
Any
copyright
is
dedicated
to
the
Public
Domain
.
https
:
/
/
creativecommons
.
org
/
publicdomain
/
zero
/
1
.
0
/
*
/
"
use
strict
"
;
const
{
MemoriesSchedulers
}
=
ChromeUtils
.
importESModule
(
"
moz
-
src
:
/
/
/
browser
/
components
/
aiwindow
/
models
/
memories
/
MemoriesSchedulers
.
sys
.
mjs
"
)
;
const
{
MemoriesManager
}
=
ChromeUtils
.
importESModule
(
"
moz
-
src
:
/
/
/
browser
/
components
/
aiwindow
/
models
/
memories
/
MemoriesManager
.
sys
.
mjs
"
)
;
const
{
MemoryStore
}
=
ChromeUtils
.
importESModule
(
"
moz
-
src
:
/
/
/
browser
/
components
/
aiwindow
/
services
/
MemoryStore
.
sys
.
mjs
"
)
;
const
{
PREF_GENERATE_MEMORIES_FROM_HISTORY
PREF_GENERATE_MEMORIES_FROM_CONVERSATION
}
=
ChromeUtils
.
importESModule
(
"
moz
-
src
:
/
/
/
browser
/
components
/
aiwindow
/
models
/
memories
/
MemoriesConstants
.
sys
.
mjs
"
)
;
const
{
AIWindowTestUtils
}
=
ChromeUtils
.
importESModule
(
"
resource
:
/
/
testing
-
common
/
AIWindowTestUtils
.
sys
.
mjs
"
)
;
const
{
PlacesTestUtils
}
=
ChromeUtils
.
importESModule
(
"
resource
:
/
/
testing
-
common
/
PlacesTestUtils
.
sys
.
mjs
"
)
;
const
PREF_FIRSTRUN_HAS_COMPLETED
=
"
browser
.
smartwindow
.
firstrun
.
hasCompleted
"
;
const
PREF_TOS_CONSENT_TIME
=
"
browser
.
smartwindow
.
tos
.
consentTime
"
;
const
PREF_LAST_USAGE_TIME
=
"
browser
.
smartwindow
.
lastSmartWindowUsageTime
"
;
const
HOUR_MS
=
60
*
60
*
1000
;
const
DAY_MS
=
24
*
HOUR_MS
;
/
/
Mirrors
the
MEMORIES_SCHEDULER_COOLDOWN_MS
and
MEMORIES_MAINTENANCE_INTERVAL_MS
/
/
defaults
.
Both
are
read
into
module
-
level
consts
at
import
time
so
tests
/
/
steer
the
clocks
with
the
scheduler
'
s
testing
helpers
rather
than
prefs
.
const
COOLDOWN_MS
=
4
*
HOUR_MS
;
const
MAINTENANCE_INTERVAL_MS
=
4
*
HOUR_MS
;
let
gAIWindow
;
let
gMemorySeq
=
0
;
/
*
*
*
Remove
every
memory
(
including
soft
-
deleted
ones
)
so
each
task
starts
from
a
*
known
-
empty
MemoryStore
.
*
/
async
function
clearMemories
(
)
{
const
memories
=
await
MemoryStore
.
getMemories
(
{
includeSoftDeleted
:
true
}
)
;
for
(
const
memory
of
memories
)
{
await
MemoryStore
.
hardDeleteMemory
(
memory
.
id
)
;
}
}
/
*
*
*
Store
a
memory
that
is
due
for
maintenance
(
last
aged
two
days
ago
)
and
*
strong
enough
to
survive
the
decay
check
so
a
completed
maintenance
pass
is
*
observable
as
a
bumped
updated_at
rather
than
a
deletion
.
*
*
returns
{
Promise
<
{
id
:
string
updatedAt
:
number
}
>
}
The
stored
memory
'
s
id
and
updated_at
.
*
/
async
function
addDueMemory
(
)
{
const
twoDaysAgo
=
Date
.
now
(
)
-
2
*
DAY_MS
;
const
memory
=
await
MemoryStore
.
addMemory
(
{
memory_summary
:
Scheduler
test
memory
{
gMemorySeq
+
+
}
created_at
:
twoDaysAgo
updated_at
:
twoDaysAgo
last_accessed
:
twoDaysAgo
lifetime_accessed_count
:
100
merge_count
:
5
}
)
;
return
{
id
:
memory
.
id
updatedAt
:
memory
.
updated_at
}
;
}
/
*
*
*
Read
a
memory
'
s
maintenance
clock
after
a
tick
.
*
*
param
{
string
}
id
The
memory
ID
*
returns
{
Promise
<
?
number
>
}
updated_at
or
null
if
the
memory
is
gone
.
*
/
async
function
getUpdatedAt
(
id
)
{
const
[
memory
]
=
await
MemoryStore
.
getMemories
(
{
includeSoftDeleted
:
true
memoryIds
:
new
Set
(
[
id
]
)
}
)
;
return
memory
?
.
updated_at
?
?
null
;
}
/
*
*
*
Record
a
page
visit
and
wait
for
the
resulting
"
page
-
visited
"
notification
to
*
have
been
dispatched
arming
the
scheduler
'
s
browsing
trigger
.
*
*
param
{
string
}
url
The
URL
to
record
a
visit
for
*
/
async
function
visitPage
(
url
)
{
const
visited
=
new
Promise
(
resolve
=
>
{
const
listener
=
(
)
=
>
{
PlacesUtils
.
observers
.
removeListener
(
[
"
page
-
visited
"
]
listener
)
;
resolve
(
)
;
}
;
PlacesUtils
.
observers
.
addListener
(
[
"
page
-
visited
"
]
listener
)
;
}
)
;
await
PlacesTestUtils
.
addVisits
(
url
)
;
await
visited
;
}
add_setup
(
async
function
(
)
{
await
clearMemories
(
)
;
await
PlacesUtils
.
history
.
clear
(
)
;
/
/
The
scheduler
is
a
singleton
shared
with
the
rest
of
the
browser
.
Drop
any
/
/
instance
an
earlier
test
file
left
behind
so
each
task
here
builds
its
own
.
MemoriesSchedulers
.
stop
(
)
;
/
/
Hold
both
sources
off
while
the
window
opens
.
await
SpecialPowers
.
pushPrefEnv
(
{
set
:
[
[
PREF_GENERATE_MEMORIES_FROM_HISTORY
false
]
[
PREF_GENERATE_MEMORIES_FROM_CONVERSATION
false
]
]
}
)
;
gAIWindow
=
await
AIWindowTestUtils
.
openAIWindow
(
)
;
await
BrowserTestUtils
.
waitForMutationCondition
(
gAIWindow
.
document
.
documentElement
{
attributes
:
true
}
(
)
=
>
AIWindowTestUtils
.
isAIWindow
(
gAIWindow
)
)
;
registerCleanupFunction
(
async
(
)
=
>
{
MemoriesSchedulers
.
stop
(
)
;
await
BrowserTestUtils
.
closeWindow
(
gAIWindow
)
;
await
clearMemories
(
)
;
await
PlacesUtils
.
history
.
clear
(
)
;
/
/
Stamped
by
AIWindow
when
the
window
becomes
active
.
Services
.
prefs
.
clearUserPref
(
PREF_LAST_USAGE_TIME
)
;
}
)
;
}
)
;
/
*
*
*
Run
a
task
against
a
freshly
constructed
scheduler
with
every
enablement
gate
*
genuinely
satisfied
:
a
real
AI
Window
is
open
and
the
source
firstrun
and
*
ToS
consent
prefs
are
really
set
.
*
*
Maintenance
is
spied
rather
than
stubbed
so
it
runs
for
real
against
the
*
MemoryStore
and
tasks
can
assert
both
that
it
was
reached
and
what
it
did
.
*
Generation
and
merging
are
the
only
stubs
for
now
.
Both
are
tested
in
*
browser_MemoriesManager
.
js
.
*
*
Each
task
gets
a
new
scheduler
so
#
lastMaintenanceMs
always
starts
at
0
*
and
the
first
tick
is
due
for
maintenance
unless
the
task
says
otherwise
.
*
*
param
{
object
}
options
*
param
{
number
}
options
.
watermarkMs
Session
-
memory
watermark
to
persist
*
param
{
boolean
}
[
options
.
conversationEnabled
]
Whether
the
chat
source
is
on
*
param
{
Function
}
task
Receives
{
scheduler
sandbox
maintenance
generate
merge
}
*
/
async
function
withScheduler
(
{
watermarkMs
conversationEnabled
=
false
}
task
)
{
/
/
Clear
the
browser
state
and
set
prefs
clean
await
clearMemories
(
)
;
await
SpecialPowers
.
pushPrefEnv
(
{
set
:
[
[
PREF_GENERATE_MEMORIES_FROM_HISTORY
true
]
[
PREF_GENERATE_MEMORIES_FROM_CONVERSATION
conversationEnabled
]
[
PREF_FIRSTRUN_HAS_COMPLETED
true
]
[
PREF_TOS_CONSENT_TIME
Math
.
floor
(
Date
.
now
(
)
/
1000
)
]
]
}
)
;
await
MemoriesManager
.
setLastSessionMemoryTimestamp
(
watermarkMs
)
;
const
sandbox
=
sinon
.
createSandbox
(
)
;
const
maintenance
=
sandbox
.
spy
(
MemoriesManager
"
runMemoryMaintenance
"
)
;
const
generate
=
sandbox
.
stub
(
MemoriesManager
"
generateMemoriesFromSessions
"
)
.
resolves
(
[
]
)
;
const
merge
=
sandbox
.
stub
(
MemoriesManager
"
mergeMemories
"
)
.
resolves
(
)
;
/
/
Restart
the
memories
scheduler
clean
MemoriesSchedulers
.
stop
(
)
;
const
scheduler
=
MemoriesSchedulers
.
maybeRunAndSchedule
(
)
;
Assert
.
ok
(
scheduler
"
Scheduler
should
start
with
a
real
AI
Window
open
and
every
pref
set
.
"
)
;
/
/
Run
the
test
task
and
reset
after
completed
try
{
await
task
(
{
scheduler
sandbox
maintenance
generate
merge
}
)
;
}
finally
{
MemoriesSchedulers
.
stop
(
)
;
sandbox
.
restore
(
)
;
await
SpecialPowers
.
popPrefEnv
(
)
;
await
MemoriesManager
.
setLastSessionMemoryTimestamp
(
0
)
;
await
PlacesUtils
.
history
.
clear
(
)
;
}
}
/
*
*
*
A
full
tick
with
the
cooldown
cleared
and
a
real
page
visit
pending
runs
*
maintenance
generation
and
merging
in
that
order
and
the
maintenance
pass
*
actually
ages
the
store
.
*
/
add_task
(
async
function
test_full_run_order
(
)
{
await
withScheduler
(
{
watermarkMs
:
Date
.
now
(
)
-
COOLDOWN_MS
-
HOUR_MS
}
async
(
{
scheduler
maintenance
generate
merge
}
)
=
>
{
const
memory
=
await
addDueMemory
(
)
;
await
visitPage
(
"
https
:
/
/
example
.
com
/
scheduler
-
tick
"
)
;
await
scheduler
.
runNowForTesting
(
)
;
Assert
.
ok
(
maintenance
.
calledOnce
"
Maintenance
should
have
run
.
"
)
;
Assert
.
ok
(
generate
.
calledOnce
"
Generation
should
have
run
.
"
)
;
Assert
.
ok
(
merge
.
calledOnce
"
Merging
should
have
run
.
"
)
;
sinon
.
assert
.
callOrder
(
maintenance
generate
merge
)
;
Assert
.
greater
(
await
getUpdatedAt
(
memory
.
id
)
memory
.
updatedAt
"
Maintenance
should
have
aged
the
memory
for
real
.
"
)
;
}
)
;
}
)
;
/
*
*
*
Maintenance
is
on
its
own
clock
so
it
runs
even
when
the
generation
cooldown
*
has
not
elapsed
.
*
/
add_task
(
async
function
test_maintenance_runs_when_cooldown_unmet
(
)
{
await
withScheduler
(
/
/
Generation
watermark
set
to
now
so
generation
should
not
run
only
maintenance
{
watermarkMs
:
Date
.
now
(
)
}
async
(
{
scheduler
maintenance
generate
merge
}
)
=
>
{
const
memory
=
await
addDueMemory
(
)
;
await
visitPage
(
"
https
:
/
/
example
.
com
/
cooldown
-
unmet
"
)
;
await
scheduler
.
runNowForTesting
(
)
;
Assert
.
ok
(
maintenance
.
calledOnce
"
Maintenance
should
run
despite
the
generation
cooldown
.
"
)
;
Assert
.
ok
(
generate
.
notCalled
"
Generation
should
be
held
by
the
cooldown
.
"
)
;
Assert
.
ok
(
merge
.
notCalled
"
Merging
should
be
held
by
the
cooldown
.
"
)
;
Assert
.
greater
(
await
getUpdatedAt
(
memory
.
id
)
memory
.
updatedAt
"
The
memory
should
have
been
aged
while
generation
was
held
.
"
)
;
}
)
;
}
)
;
/
*
*
*
Maintenance
also
ignores
the
evidence
accumulation
check
that
gates
*
generation
and
merging
.
*
/
add_task
(
async
function
test_maintenance_runs_when_no_trigger_met
(
)
{
await
withScheduler
(
/
/
Generation
watermark
set
long
enough
that
the
gate
should
pass
{
watermarkMs
:
Date
.
now
(
)
-
COOLDOWN_MS
-
HOUR_MS
}
async
(
{
scheduler
maintenance
generate
merge
}
)
=
>
{
const
memory
=
await
addDueMemory
(
)
;
/
/
No
page
visited
and
the
chat
source
is
off
so
generation
shouldn
'
t
run
/
/
despite
the
watermark
being
old
enough
await
scheduler
.
runNowForTesting
(
)
;
Assert
.
ok
(
maintenance
.
calledOnce
"
Maintenance
should
run
with
no
generation
trigger
met
.
"
)
;
Assert
.
ok
(
generate
.
notCalled
"
Generation
needs
a
trigger
.
"
)
;
Assert
.
ok
(
merge
.
notCalled
"
Merging
needs
a
trigger
.
"
)
;
Assert
.
greater
(
await
getUpdatedAt
(
memory
.
id
)
memory
.
updatedAt
"
The
memory
should
have
been
aged
with
no
trigger
armed
.
"
)
;
}
)
;
}
)
;
/
*
*
*
Maintenance
should
run
based
on
its
own
clock
across
scheduler
ticks
.
Make
*
sure
maintenance
doesn
'
t
trigger
on
ticks
within
its
cooldown
.
*
/
add_task
(
async
function
test_maintenance_skipped_within_interval
(
)
{
await
withScheduler
(
{
watermarkMs
:
Date
.
now
(
)
-
COOLDOWN_MS
-
HOUR_MS
}
async
(
{
scheduler
maintenance
}
)
=
>
{
await
addDueMemory
(
)
;
await
scheduler
.
runNowForTesting
(
)
;
await
scheduler
.
runNowForTesting
(
)
;
await
scheduler
.
runNowForTesting
(
)
;
Assert
.
ok
(
maintenance
.
calledOnce
"
Maintenance
should
run
once
across
three
back
-
to
-
back
ticks
.
"
)
;
}
)
;
}
)
;
/
*
*
*
Once
the
maintenance
interval
has
elapsed
the
next
tick
runs
it
again
.
*
/
add_task
(
async
function
test_maintenance_runs_again_after_interval
(
)
{
await
withScheduler
(
{
watermarkMs
:
Date
.
now
(
)
}
async
(
{
scheduler
maintenance
}
)
=
>
{
await
addDueMemory
(
)
;
await
scheduler
.
runNowForTesting
(
)
;
Assert
.
ok
(
maintenance
.
calledOnce
"
Maintenance
should
run
on
first
tick
.
"
)
;
/
/
Set
maintenance
watermark
beyond
check
threshold
so
it
runs
again
scheduler
.
setLastMaintenanceMsForTesting
(
Date
.
now
(
)
-
MAINTENANCE_INTERVAL_MS
-
HOUR_MS
)
;
await
scheduler
.
runNowForTesting
(
)
;
Assert
.
equal
(
maintenance
.
callCount
2
"
Maintenance
should
run
again
once
its
interval
has
elapsed
.
"
)
;
}
)
;
}
)
;
/
*
*
*
Maintenance
stays
behind
the
LLM
429
backoff
gate
.
It
makes
no
LLM
call
but
a
*
backed
-
off
scheduler
is
deliberately
left
fully
idle
.
*
/
add_task
(
async
function
test_maintenance_skipped_during_backoff
(
)
{
await
withScheduler
(
{
watermarkMs
:
Date
.
now
(
)
-
COOLDOWN_MS
-
HOUR_MS
}
async
(
{
scheduler
maintenance
generate
}
)
=
>
{
const
memory
=
await
addDueMemory
(
)
;
await
visitPage
(
"
https
:
/
/
example
.
com
/
backoff
"
)
;
/
/
Set
the
429
backoff
so
maintenance
generation
and
merging
should
not
run
scheduler
.
setBackoffUntilMsForTesting
(
Date
.
now
(
)
+
HOUR_MS
)
;
await
scheduler
.
runNowForTesting
(
)
;
Assert
.
ok
(
maintenance
.
notCalled
"
Backoff
should
skip
maintenance
.
"
)
;
Assert
.
ok
(
generate
.
notCalled
"
Backoff
should
skip
generation
.
"
)
;
Assert
.
equal
(
await
getUpdatedAt
(
memory
.
id
)
memory
.
updatedAt
"
The
memory
should
be
untouched
while
backed
off
.
"
)
;
scheduler
.
setBackoffUntilMsForTesting
(
0
)
;
await
scheduler
.
runNowForTesting
(
)
;
Assert
.
ok
(
maintenance
.
calledOnce
"
Maintenance
should
resume
once
the
backoff
clears
.
"
)
;
}
)
;
}
)
;
/
*
*
*
A
failing
maintenance
pass
is
contained
:
generation
still
runs
and
the
*
attempt
is
stamped
so
the
next
tick
does
not
immediately
retry
.
*
/
add_task
(
async
function
test_maintenance_failure_is_contained
(
)
{
await
withScheduler
(
{
watermarkMs
:
Date
.
now
(
)
-
COOLDOWN_MS
-
HOUR_MS
}
async
(
{
scheduler
sandbox
maintenance
generate
merge
}
)
=
>
{
/
/
Stub
getMemories
to
simulate
a
broken
maintenance
run
sandbox
.
stub
(
MemoryStore
"
getMemories
"
)
.
rejects
(
new
Error
(
"
Memory
store
unavailable
"
)
)
;
await
visitPage
(
"
https
:
/
/
example
.
com
/
maintenance
-
failure
"
)
;
await
scheduler
.
runNowForTesting
(
)
;
Assert
.
ok
(
maintenance
.
calledOnce
"
Maintenance
should
have
been
attempted
.
"
)
;
Assert
.
ok
(
generate
.
calledOnce
"
A
maintenance
failure
should
not
block
generation
.
"
)
;
Assert
.
ok
(
merge
.
calledOnce
"
A
maintenance
failure
should
not
block
merging
.
"
)
;
await
scheduler
.
runNowForTesting
(
)
;
Assert
.
ok
(
maintenance
.
calledOnce
"
A
failed
maintenance
pass
should
not
retry
on
the
next
tick
but
wait
until
enough
time
has
elapsed
based
on
its
watermark
.
"
)
;
}
)
;
}
)
;
